// Dead code removed: bitstream.rs was a custom BitWriter/BitReader that was never used.
// The decoder uses the bitstream-io crate directly instead.
// Module kept as empty to avoid breaking existing references during transition.
